apiVersion: v1
kind: ServiceAccount
metadata:
  name: hello-world
  namespace: hello-world
  annotations:
    # Replace with your AGC managed identity client ID before deployment.
    azure.workload.identity/client-id: "<YOUR_AGC_IDENTITY_CLIENT_ID>"
  labels:
    azure.workload.identity/use: "true"
