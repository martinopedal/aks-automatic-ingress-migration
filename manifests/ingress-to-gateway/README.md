# Ingress to Gateway and HTTPRoute manifest catalog

Each file contains a source Ingress example and a target Gateway API example.

- `tls.yaml`: TLS termination.
- `path-rewrite.yaml`: Path rewrite.
- `header-rewrite.yaml`: Header rewrite.
- `weighted-traffic.yaml`: Weighted traffic split.
